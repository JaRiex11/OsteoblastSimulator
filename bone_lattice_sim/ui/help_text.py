"""Тексты подсказок для GUI."""

from __future__ import annotations

from bone_lattice_sim.simulation.agents import cell_type_help_text

CONFIG_HELP = f"""
<h3>Параметры симуляции</h3>

<p><b>Размер N</b> — сторона куба N×N×N пор (от 2 до 30). Число пор = N³.</p>

<p><b>Топология:</b></p>
<ul>
<li><b>regular_6</b> — ортогональные связи (6 соседей);</li>
<li><b>random</b> — те же связи, но часть каналов (throats) случайно удалена;</li>
<li><b>full_26</b> — все соседи в окне 3×3×3 (26 связей).</li>
</ul>

<p><b>Доля удаления throats</b> — только для <b>random</b>, от 0.0 до 0.99.
Например 0.3 = удалить ~30% каналов (связность восстанавливается автоматически).</p>

<p><b>Начальное размещение:</b></p>
<ul>
<li><b>center</b> — одна клетка <i>первого</i> типа из поля «Состав» в центре решётки;</li>
<li><b>random</b> — все клетки из «Состава» в случайных разных порах
(если клеток в mix больше, чем «Число пор», берётся сумма из mix).</li>
</ul>

<p><b>Состав (тип:число)</b> — через запятую или «;». Примеры:</p>
<ul>
<li><code>osteoblast:1</code></li>
<li><code>fibroblast:5</code></li>
<li><code>osteoblast:3,msc:2,fibroblast:1</code></li>
</ul>

<p>{cell_type_help_text().replace(chr(10), '<br>')}</p>

<p><b>P_mig / P_prol</b> — вероятности на один шаг (0…1) для каждого типа.
За шаг клетка делает <i>не более одного</i> действия: миграция или деление.</p>

<p><b>Seed</b> — зерно ГСЧ для воспроизводимости.</p>

<p><b>Обновление графика каждые K шагов</b> — как часто обновляются графики и 3D Viewer.</p>
"""

VIEWER_LEGEND = (
    "Цвета клеток: "
    "зелёный — osteoblast, "
    "синий — msc, "
    "красный — fibroblast, "
    "серый — пустая пора. "
    "Клетки отображаются сферами крупнее каналов решётки."
)
